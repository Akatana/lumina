package main

import "testing"

func TestMainExecution(t *testing.T) {
	// main() will print to stdout and process a small image.
	// This test just ensures that main() executes without panicking.
	main()
}
